@using CourseManagement.Models
@model CoursesViewModel
@{
    ViewData["Title"] = "Teachers";

    Pager pager = new Pager();
    int pageNo = 0;
    if (ViewBag.Pager != null)
    {
        pager = ViewBag.Pager;
        pageNo = pager.CurrentPage;
    }
}

<!DOCTYPE html>
<html lang="en">
<head>
    <title>Teachers</title>
    <meta charset="UTF-8">
</head>
<body>
    <form>
        <div class="row">
            <div class="col-6">
                <h2 class="text-info">Course Lists</h2>
            </div>
            <div class="col-6 text-right">
                <a asp-controller="Courses" asp-action="Create" class="btn btn-info">Create New Course</a>
            </div>
        </div>

        <div class="border bg-white">
            <br />
            <div>
                <table class="table table-striped border">
                    <tr class="table-secondary">
                        <th>
                            @Html.DisplayNameFor(m => m.Name)
                        </th>
                        <th>
                            @Html.DisplayNameFor(m => m.Prices)
                        </th>
                        <th>
                            @Html.DisplayNameFor(m => m.Profession)
                        </th>
                        <th>
                            @Html.DisplayNameFor(m => m.Duration)
                        </th>

                        <th>

                        </th>
                    </tr>
                    @foreach (var item in Model.Courses)
                    {
                        <tr class="table text-black">
                            <td>
                                @Html.DisplayFor(m => item.Name)
                            </td>
                            <td>
                                @Html.DisplayFor(m => item.Prices) $
                            </td>
                            <td>
                                @Html.DisplayFor(m => item.Profession)
                            </td>
                            <td>
                                @Html.DisplayFor(m => item.Duration)
                            </td>
                            <td>
                                <div class="btn-group">
                                    <a asp-action="Edit" asp-route-id="@item.ID" class="btn btn-sm btn-warning">Edit</a>
                                    <a asp-action="Delete" asp-route-id="@item.ID" class="btn btn-sm btn-danger ml-1">Delete</a>
                                    <a asp-action="Details" asp-route-id="@item.ID" class="btn btn-sm btn-primary ml-1">Details</a>

                                </div>
                            </td>
                        </tr>
                    }
                </table>
            </div>
            <partial name="Pager" model="@pager" />
        </div>
    </form>
</body>
</html>